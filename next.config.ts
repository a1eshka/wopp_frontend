import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['eeaa-150-241-116-1.ngrok-free.app'],
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
